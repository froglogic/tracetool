SET(SERVER_SOURCES
        main.cpp)

FIND_PACKAGE(Qt4 4.5.0 COMPONENTS QtCore QtNetwork REQUIRED)
INCLUDE(${QT_USE_FILE})
ADD_EXECUTABLE(traceserver ${SERVER_SOURCES})
TARGET_LINK_LIBRARIES(traceserver ${QT_LIBRARIES})

