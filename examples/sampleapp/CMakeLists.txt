ADD_EXECUTABLE(sampleapp sampleapp.cpp)
TARGET_LINK_LIBRARIES(sampleapp tracelib)
IF(MSVC)
    SET_TARGET_PROPERTIES(sampleapp PROPERTIES COMPILE_FLAGS "/FC")
ENDIF(MSVC)
SET_TARGET_PROPERTIES(sampleapp PROPERTIES COMPILE_FLAGS /I${PROJECT_SOURCE_DIR})

CONFIGURE_FILE(${PROJECT_SOURCE_DIR}/examples/sampleapp/tracelib.xml
        ./tracelib.xml
        COPYONLY)

